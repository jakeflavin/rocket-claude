# rocket-claude
A Claude Code powered financial tracker. 

## Running

This project should be served over HTTP.

From the project root, run:

```bash
npx serve .
```

Then open the URL printed by `serve`, typically:

```text
http://localhost:3000/index.html
```
